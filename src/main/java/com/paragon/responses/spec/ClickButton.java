package com.paragon.responses.spec;

/**
 * Indicates which mouse button was pressed during the click. One of {@code left}, {@code right},
 * {@code wheel}, {@code back}, or {@code forward}.
 */
public enum ClickButton {
  LEFT,
  RIGHT,
  WHEEL,
  BACK,
  FORWARD
}
