# :material-database: ErrorData

`com.paragon.messaging.whatsapp.payload.ErrorData` &nbsp;·&nbsp; **Record**

---

Represents error data in WhatsApp API responses.

**Important:** Use the `code` field for programmatic error handling, not the `title` field which is deprecated by Meta.
